# Iroh Rooms Production Beta Sign-Off

Status: PENDING RELEASE-OWNER APPROVAL

This record covers the `v0.1.0-rc.4` release candidate prepared on 2026-07-30.
It records automated evidence, manual P0 review results, and the explicit risk
acceptances required for a controlled Production Beta. Items marked PENDING
require the release owner; everything else was verified at the release commit.

> Note on the previous candidate: `v0.1.0-rc.3`'s sign-off was never completed
> (it remains PENDING RELEASE-OWNER APPROVAL, as does `v0.1.0-rc.2`'s). rc.4
> supersedes both. Approving rc.4 does not retroactively approve rc.3; if any
> cohort member is running rc.3 or rc.2, migrate them to rc.4.

## Candidate

| Field | Value |
| --- | --- |
| Version | `0.1.0-rc.4` |
| Git tag | `v0.1.0-rc.4` |
| Commit SHA | `d475f12e9ec31513fdf7c1ae5742d09bc6c20635` |
| Base of candidate | `f3906fec0c106dbcf16d7b3716194ea78de532bf` (main, 72 commits after `v0.1.0-rc.3`) |
| Date | 2026-07-30 |
| Release owner | Sekou Oumar KONE |
| Release-owner approval | PENDING |
| Security reviewer | No independent reviewer for this candidate; per-PR automated reviews only (carries WAIVER-PBETA-0003). The v2 governance/fork audit (`docs/audits/v2-governance-fork-audit-2026-07-30.md`, APPROVED) covers the non-shipping `iroh-rooms-v2-core` crate, **not** the v1 shipping surface in this candidate |
| Protocol/storage reviewer | PENDING — no wire, canonical-CBOR, signature, membership-fold, authorization, or SQLite schema change in this candidate; release owner to accept |
| Networking reviewer | PENDING — no transport-layer change in shipped binaries: the gossip overlay (issue #171) and the paired 5 -> 40 ceiling raise (issue #173) are compiled but **not enabled**, so rc.4 runs rc.3's full-mesh topology and cap of 5. Gate A evidence carries over on the same basis rc.3 used. Release owner to accept |
| Platforms exercised | Ubuntu 24.04.4 LTS, Linux 6.17 `aarch64-unknown-linux-gnu` (both automated gates); artifacts to be built by the `release` workflow on a native runner per triple. The other three archives will be checksum-verified but not executed on their native hardware |
| Rust toolchain | `rustc 1.97.1 (8bab26f4f 2026-07-14)` on the gate host |
| SDK publication status | Source/workspace only; `crates/iroh-rooms` remains `publish = false` |
| Local storage posture | Plaintext local data, accepted for trusted-local-machine beta only by ADR-0001 |
| Invite revocation posture | No native ticket-specific revocation; bounded leaked-ticket model accepted for beta only by ADR-0002 |

## Automated Gates

Run on `aarch64-unknown-linux-gnu` at commit `d475f12e9ec31513fdf7c1ae5742d09bc6c20635`:

| Gate | Result | Evidence |
| --- | --- | --- |
| `scripts/verify.sh` | PASS | Printed `verify.sh: PASS` inside the release-readiness run |
| `scripts/release-readiness.sh` | PASS | `release-readiness: READY`; all six online loopback tiers PASS, none skipped |
| `scripts/production-readiness.sh` | PASS with 1 warning | `production-readiness: AUTOMATED PREFLIGHT PASS (1 warnings)`; the warning is the `publish = false` SDK status covered by WAIVER-PBETA-0005 |
| Compatibility fixture gate | PASS | `cargo test -p iroh-rooms-core --features store --test compatibility`: 7 passed, 0 failed, 1 ignored (was 4 passed at rc.3; the three new tests are the rc.3 fixture) |
| rc.3 -> rc.4 data continuity | PASS | rc.3-created `IROH_ROOMS_HOME` opened under rc.4: `identity show`, `room members`, and `room tail --offline` all correct; `PRAGMA integrity_check` OK; `user_version` stayed 2; before/after SQL dump byte-identical. Reverse direction also verified — an rc.3 binary read back a store rc.4 had written, including an rc.4-authored event |
| Backup/restore smoke test | PASS with doc fixes | Documented flow executed end to end: restore correctly discarded a post-backup message, `integrity_check` OK, read-only verification commands passed, writes resumed, and `$IROH_ROOMS_HOME.before-restore` was preserved. Three documentation defects found and fixed in this candidate — see P0.5 |
| Gossip N=40 matrix | RUN — **AC FAILS**, feature held back | `crates/spike-N40/results/results-gossip.md`, regenerated through the committed harness. The spec's Phase B criterion (no cascade at 1 event/s, connectedness >95%, delivery >95% at every N) is not met: cascade at 1 event/s with both figures under 95%, at N=5 as well as N=40. Consequence for this candidate: `gossip_overlay` and the paired cap raise are **not enabled** in the shipped CLI or SDK, so rc.4 ships rc.3's topology and ceiling |
| Artifact build | `<FILL AT TAG>` | `release` workflow at the `v0.1.0-rc.4` tag, one native runner per triple |
| Binary version | PASS (local); artifact PENDING | A release build at the candidate commit prints `iroh-rooms 0.1.0-rc.4`. The published archives must still be checked at the tag |
| Artifact checksum | `<FILL AT TAG>` | `shasum -a 256 -c` on all four published archives |

## Artifact

| Archive | Platform | SHA-256 |
| --- | --- | --- |
| `<FILL AT TAG>` | `x86_64-apple-darwin` | `<FILL AT TAG>` |
| `<FILL AT TAG>` | `aarch64-apple-darwin` | `<FILL AT TAG>` |
| `<FILL AT TAG>` | `x86_64-unknown-linux-gnu` | `<FILL AT TAG>` |
| `<FILL AT TAG>` | `aarch64-unknown-linux-gnu` | `<FILL AT TAG>` |

| Field | Value |
| --- | --- |
| Release notes | `docs/releases/v0.1.0-rc.4-release-notes.md` |
| Project signing | Not signed; checksum-only beta artifact (WAIVER-PBETA-0004) |

## Manual P0 Sign-Offs

| P0 | Decision | Evidence | Human action required |
| --- | --- | --- | --- |
| P0.1 Preview gate is green | Green | `release-readiness: READY` at `d475f12`; no skipped online tier | Release owner confirms |
| P0.2 Real-network evidence is current | Carried over | 2026-07-03/04 Gate A corpus + 2026-07-07 refresh. Valid on the same basis rc.3 used: no transport-layer performance change in shipped binaries, because the gossip overlay is compiled but not enabled | Release owner re-accepts WAIVER-PBETA-0001 and WAIVER-PBETA-0002 |
| P0.3 Threat model and security review | Updated | `docs/security/threat-model.md` gains row T26 (unadmitted peer on the gossip overlay), controlled by the admission wrapper on `GOSSIP_ALPN`; ADR-0001/0002/0003 unchanged. rc.3's residuals #121 and #126 are closed in this candidate | Release owner re-accepts WAIVER-PBETA-0003 |
| P0.4 Invite and access revocation story | Signed off with ADR-0002 | Test corpus still passes; no revocation change in this candidate | Bounded leaked-ticket model accepted for beta only |
| P0.5 Local data handling | Smoke test run; runbook corrected | The documented backup and restore flows were executed end to end and work. Three defects in the runbook were found and fixed for this candidate: (1) the `data-handling.md` restore example named `iroh-rooms-backup.tar.gz`, which no documented backup command produces — copy-pasting it failed; (2) both documented backup commands wrote a **group- and world-readable** archive containing plaintext `identity.secret`, despite the app enforcing 0700 on the data directory and 0600 on the secret — both now run under `umask 077`; (3) `install-uninstall.md`'s rollback ran `rm -rf "$IROH_ROOMS_HOME"` *before* extracting, destroying the only remaining copy if the backup was bad — now moves the directory aside, matching `data-handling.md`'s stated "explicit and reversible" policy | Release owner confirms the corrected runbook |
| P0.6 Persistent audit and diagnostics | Improved | Issue #122 closed: capability-proof accepts and rejects now land in `audit.ndjson`. Three previously silent anti-amplification cliffs now raise CRITICAL trust decisions that survive restart | Local-only, best-effort audit posture accepted |
| P0.7 Compatibility and migration | Satisfied | The item rc.3 never completed is now closed. v1 wire + v1 schema fixtures still pass; the required previous-candidate fixture is committed at `crates/iroh-rooms-core/tests/fixtures/rc3/`, generated by the **published** rc.3 `aarch64-unknown-linux-gnu` binary whose checksum verified three ways (published `.sha256`, rc.3 release-notes table, GitHub asset digest). The fixture follows the existing v1 byte/schema style and carries no secrets by construction — `identity.secret` and the invite ticket are excluded, and `member.joined` is deliberately omitted because its content embeds `capability_secret`. Runtime open test recorded above; no migration occurs (`user_version` 2 -> 2) | Release owner confirms |
| P0.8 Release operations | Prepared | Versioned notes, checksum flow, install/uninstall/rollback docs, bug template all present; artifacts built by the tag-triggered `release` workflow | Release owner approves |

## Current Waivers

Carried over from v0.1.0-rc.3, re-acceptance required at approval:

| Waiver | Owner | Expires | Rationale |
| --- | --- | --- | --- |
| WAIVER-PBETA-0001: cellular forced-relay throughput caveat | Release owner | Next Production Beta candidate or before GA, whichever comes first | Unchanged |
| WAIVER-PBETA-0002: missing home-NAT -> CGNAT reverse leg | Release owner | Next real-NAT refresh or before GA, whichever comes first | Unchanged |
| WAIVER-PBETA-0003: no independent security review completed | Release owner | Before public beta expansion or GA | Unchanged; per-PR automated review is not an external security sign-off |
| WAIVER-PBETA-0004: checksum-only unsigned artifact | Release owner | Before GA | Unchanged |
| WAIVER-PBETA-0005: SDK source/workspace-only | Release owner | Before SDK packaging/publication milestone | Unchanged |

No new waiver is required for this candidate. The one that would have been —
covering a 40-active-member ceiling evidenced on loopback only — is unnecessary
because the ceiling raise is held back rather than shipped, so rc.4's real-network
posture is identical to rc.3's.

## Release-Owner Confirmation

PENDING. Template for approval:

```text
I approve v0.1.0-rc.4 for controlled Production Beta and re-accept
WAIVER-PBETA-0001 through WAIVER-PBETA-0005, plus the ADR-0001 plaintext local
storage posture, ADR-0002 bounded leaked-ticket model, and ADR-0003 local-only
audit posture.
```

Approval additionally confirms for this candidate:

1. The rc.3 -> rc.4 data-continuity check was run and recorded (P0.7) — the
   item rc.3's own sign-off left open.
2. The backup/restore smoke test was run and recorded (P0.5).
3. The mixed-version position is understood: rc.3 and rc.4 peers interoperate
   normally and members may upgrade one at a time, because this candidate
   changes no wire format, ALPN, schema, topology, or ceiling.
4. The gossip overlay and its paired 40-member ceiling are held back because
   the spec's own Phase B acceptance criterion fails against the committed
   harness. Re-enabling them requires establishing whether that result is a
   harness fidelity gap or a real defect in the overlay, and then a Phase B
   re-run that genuinely passes — at N=10 and N=20 as well, which the original
   evidence never covered.
5. Issues #119, #121, #122, #126, and #136 are closed in this candidate. The
   remaining residuals — pure-chat gaps deeper than the 1024-event backfill
   bound, wide-DAG duplicate re-serves pending range reconciliation (#102,
   #155), and legacy rc.2 oversized events — are accepted as tracked beta
   limitations.
6. The downstream `qualification/jeliya-*` pin channel is understood and its
   disposition decided — see `docs/operations/release-operations.md`.

## Verdict

PENDING RELEASE-OWNER APPROVAL.
