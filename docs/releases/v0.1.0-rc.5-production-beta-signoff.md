# Iroh Rooms Production Beta Sign-Off

Status: PENDING RELEASE-OWNER APPROVAL

This record covers the `v0.1.0-rc.5` release candidate prepared on 2026-08-01.
It records automated evidence, manual P0 review results, and the explicit risk
acceptances required for a controlled Production Beta. Items marked PENDING
require the release owner; everything else was verified at the release commit.

> Note on previous candidates: `v0.1.0-rc.4`, `v0.1.0-rc.3`, and `v0.1.0-rc.2`
> sign-offs were never completed (they remain PENDING RELEASE-OWNER APPROVAL).
> rc.5 supersedes all three. Approving rc.5 does not retroactively approve
> them; if any cohort member is running rc.2/rc.3/rc.4, migrate them to rc.5.

## Candidate

| Field | Value |
| --- | --- |
| Version | `0.1.0-rc.5` |
| Git tag | `v0.1.0-rc.5` |
| Commit SHA | `dc6833cc4690bc23d9d1cdb95841e8c1aec5514c` (merge of PR #205 onto main) |
| Base of candidate | `75d0e286379821a191417e37ee3fbc6906981fae` (main, 50 commits after `v0.1.0-rc.4`) |
| Date | 2026-08-01 |
| Release owner | Sekou Oumar KONE |
| Release-owner approval | PENDING |
| Security reviewer | No independent reviewer for this candidate; per-PR automated reviews only (carries WAIVER-PBETA-0003). The v2 governance/fork audit (`docs/audits/v2-governance-fork-audit-2026-07-30.md`, APPROVED) covers the non-shipping `iroh-rooms-v2-core` crate, **not** the v1 shipping surface in this candidate. The #191 rotation lifecycle is the largest security-relevant change and had per-PR automated review across steps 1–8 |
| Protocol/storage reviewer | PENDING — **wire and SQLite surface DO change in this candidate** (additive): `content.encrypted` envelope + `member.removed.rotation` field + `member.key_distribution` event join the closed content registry (strict additions, rejected as unknown by pre-floor peers), and a re-derivable `room_keys` cache table is added (idempotent DDL, `user_version` stays 2). Release owner to accept |
| Networking reviewer | PENDING — no transport-layer performance change in shipped binaries: the gossip overlay (issue #171) and the paired 5 -> 40 ceiling raise (issue #173) are compiled but **not enabled**, so rc.5 runs rc.4's full-mesh topology and cap of 5. Gate A evidence carries over on the same basis rc.3/rc.4 used. Release owner to accept |
| Platforms exercised | Ubuntu 24.04.4 LTS, Linux 6.17 `aarch64-unknown-linux-gnu` (all three automated gates). Artifacts to be built by the `release` workflow on a native runner per triple (run URL recorded below after the tag push) |
| Rust toolchain | `rustc 1.97.1 (8bab26f4f 2026-07-14)` on the gate host |
| SDK publication status | Source/workspace only; `crates/iroh-rooms` remains `publish = false` |
| Local storage posture | Plaintext local data, accepted for trusted-local-machine beta only by ADR-0001. The `room_keys` epoch-key cache lives inside `rooms.db`, so the database is a secret (T28) |
| Invite revocation posture | No native ticket-specific revocation; bounded leaked-ticket model accepted for beta only by ADR-0002 |

## Automated Gates

Run on `aarch64-unknown-linux-gnu` at the release commit:

| Gate | Result | Evidence |
| --- | --- | --- |
| `scripts/verify.sh` | PASS | exit 0; zero failures across the full workspace |
| `scripts/release-readiness.sh` | PASS | `release-readiness: READY`; all six online loopback tiers PASS, none skipped |
| `scripts/production-readiness.sh` | PASS with 1 warning | `production-readiness: AUTOMATED PREFLIGHT PASS (1 warnings)`; the warning is the `publish = false` SDK status covered by WAIVER-PBETA-0005 |
| Compatibility fixture gate | PASS | `cargo test -p iroh-rooms-core --features store --test compatibility`: 7 passed, 0 failed, 1 ignored |
| rc.4 -> rc.5 data continuity | PASS | rc.4-created `IROH_ROOMS_HOME` opened under rc.5: `identity show`, `room members`, and `room tail --offline` all correct; `PRAGMA integrity_check` OK; `user_version` stayed 2; `room_keys` table created idempotently and empty (plaintext fixture room had no rotation). Recorded below |
| Backup/restore smoke test | PENDING | Documented flow to be executed end to end by the release owner (see P0.5) |
| Gossip N=40 matrix | RUN — root cause fixed on loopback, feature still held back | `crates/spike-N40/results/results-gossip.md`. The three stacked defects behind the rc.4 collapse are fixed; the corrected loopback matrix is clean at every measured cell. Consequence for this candidate: `gossip_overlay` and the paired cap raise remain **not enabled** — re-enablement gates on real-network overlay evidence and the admin fail-closed recovery story |
| Artifact build | PASS | `release` workflow at the `v0.1.0-rc.5` tag, one native runner per triple, all four green: tag-push run <https://github.com/kortiene/iroh-room/actions/runs/30728170803> built the artifacts; the attach step ran in <https://github.com/kortiene/iroh-room/actions/runs/30728544032> (the GitHub Release was created between the two). All four built with `rustc 1.97.1 (8bab26f4f 2026-07-14)` |
| Binary version | PASS | The published `aarch64-unknown-linux-gnu` archive prints `iroh-rooms 0.1.0-rc.5`; its `RELEASE-MANIFEST.txt` records commit `dc6833cc4690` with no `-dirty` suffix |
| Artifact checksum | PASS (1 of 4 executed) | `sha256sum -c` OK on the published `aarch64-unknown-linux-gnu` archive, extracted and run on native hardware. The other three are recorded from the publisher's `.sha256` files and were not independently re-downloaded |

## rc.4 -> rc.5 Data Continuity (P0.7)

Executed on `aarch64-unknown-linux-gnu` with the published rc.4
`aarch64-unknown-linux-gnu` binary (checksum verified against this
repository's rc.4 release notes):

- A fresh `IROH_ROOMS_HOME` was created with the rc.4 binary: identity
  created, a room created, a message posted.
- The same data directory was then opened with the rc.5 build: `identity
  show`, `room members`, and `room tail --offline` all return the rc.4-created
  contents unchanged.
- `PRAGMA integrity_check` returns `ok`; `PRAGMA user_version` is `2` before
  and after (no migration is performed). The additive `room_keys` table is
  created idempotently and is empty, because the plaintext fixture room never
  enabled rotation.

## Artifact

Built by the `release` workflow at tag `v0.1.0-rc.5` (`dc6833c`) and attached
to the release:

| Archive | Platform | SHA-256 |
| --- | --- | --- |
| `iroh-rooms-0.1.0-rc.5-x86_64-apple-darwin-dc6833cc4690.tar.gz` | `x86_64-apple-darwin` | `71084085ec1b3bb6273377ec734506ff59946a037123b820db4ba6c57cd0bc35` |
| `iroh-rooms-0.1.0-rc.5-aarch64-apple-darwin-dc6833cc4690.tar.gz` | `aarch64-apple-darwin` | `3bd2bb5df4f3b34535d65e6d1019e65b13d0792b871dbb966de613a74abf163b` |
| `iroh-rooms-0.1.0-rc.5-x86_64-unknown-linux-gnu-dc6833cc4690.tar.gz` | `x86_64-unknown-linux-gnu` | `532db1629ec0211240170516c0505cdc747938bed5096a1cf57e34ca04f48b9c` |
| `iroh-rooms-0.1.0-rc.5-aarch64-unknown-linux-gnu-dc6833cc4690.tar.gz` | `aarch64-unknown-linux-gnu` | `28c5afd44294ac0be079a3b5d628efe5d35586a0dcc48c0ac6a73645e1d7a329` |

| Field | Value |
| --- | --- |
| Release notes | `docs/releases/v0.1.0-rc.5-release-notes.md` |
| Project signing | Not signed; checksum-only beta artifact (WAIVER-PBETA-0004) |

## Manual P0 Sign-Offs

| P0 | Decision | Evidence | Human action required |
| --- | --- | --- | --- |
| P0.1 Preview gate is green | Green | `release-readiness: READY` at the release commit; no skipped online tier | Release owner confirms |
| P0.2 Real-network evidence is current | Carried over | 2026-07-03/04 Gate A corpus + 2026-07-07 refresh. Valid on the same basis rc.3/rc.4 used: no transport-layer performance change in shipped binaries, because the gossip overlay is compiled but not enabled | Release owner re-accepts WAIVER-PBETA-0001 and WAIVER-PBETA-0002 |
| P0.3 Threat model and security review | Updated | `docs/security/threat-model.md`: T27 moves to **Controlled** for the malicious-reader case (the #191 rotation lifecycle makes post-departure content ciphertext a malicious removed device cannot read); new rows T28 (epoch key store), T29 (rotation latency / no automatic post-leave rotation), T30 (metadata-length leakage). ADR-0001/0002/0003 unchanged | Release owner re-accepts WAIVER-PBETA-0003 |
| P0.4 Invite and access revocation story | Signed off with ADR-0002 | Test corpus still passes; no revocation change in this candidate. The #194/#195 terminal-removal hardening makes `member.removed` a receipt-gated final room fact | Bounded leaked-ticket model accepted for beta only |
| P0.5 Local data handling | PENDING smoke test | The backup/restore runbook (`docs/operations/data-handling.md`, `install-uninstall.md`) was corrected in rc.4 and is unchanged here; the smoke test is to be re-executed end to end by the release owner. Note the sharper posture: `room_keys` lives inside `rooms.db`, so a backup includes the keys to all encrypted room history | Release owner confirms the runbook and re-runs the smoke test |
| P0.6 Persistent audit and diagnostics | Unchanged | Local-only, best-effort `audit.ndjson`; CRITICAL trust decisions survive restart. The rotation lifecycle adds `epoch_key_adopted` / `epoch_key_conflict` / `epoch_key_resolved` log lines; no new remote audit surface | Local-only, best-effort audit posture accepted |
| P0.7 Compatibility and migration | Satisfied | v1 wire + v1 schema fixtures still pass; the previous-candidate fixture is committed at `crates/iroh-rooms-core/tests/fixtures/rc3/`. rc.4 -> rc.5 runtime open recorded above; no migration occurs (`user_version` 2 -> 2; additive idempotent `room_keys` table) | Release owner confirms |
| P0.8 Release operations | Prepared | Versioned notes, checksum flow, install/uninstall/rollback docs, bug template all present; artifacts built by the tag-triggered `release` workflow | Release owner approves |

## Current Waivers

Carried over from v0.1.0-rc.4, re-acceptance required at approval:

| Waiver | Owner | Expires | Rationale |
| --- | --- | --- | --- |
| WAIVER-PBETA-0001: cellular forced-relay throughput caveat | Release owner | Next Production Beta candidate or before GA, whichever comes first | Unchanged |
| WAIVER-PBETA-0002: missing home-NAT -> CGNAT reverse leg | Release owner | Next real-NAT refresh or before GA, whichever comes first | Unchanged |
| WAIVER-PBETA-0003: no independent security review completed | Release owner | Before public beta expansion or GA | Unchanged; per-PR automated review is not an external security sign-off. The #191 rotation lifecycle is new security-relevant surface with no independent review |
| WAIVER-PBETA-0004: checksum-only unsigned artifact | Release owner | Before GA | Unchanged |
| WAIVER-PBETA-0005: SDK source/workspace-only | Release owner | Before SDK packaging/publication milestone | Unchanged |

No new waiver is required for this candidate. The one that would have been —
covering a 40-active-member ceiling evidenced on loopback only — is unnecessary
because the ceiling raise is still held back rather than shipped, so rc.5's
real-network posture is identical to rc.3/rc.4's.

## Release-Owner Confirmation

PENDING. Template for approval:

```text
I approve v0.1.0-rc.5 for controlled Production Beta and re-accept
WAIVER-PBETA-0001 through WAIVER-PBETA-0005, plus the ADR-0001 plaintext local
storage posture, ADR-0002 bounded leaked-ticket model, and ADR-0003 local-only
audit posture.
```

Approval additionally confirms for this candidate:

1. The rc.4 -> rc.5 data-continuity check was run and recorded (P0.7).
2. The backup/restore smoke test was re-run and recorded (P0.5).
3. The mixed-version position is understood: **plaintext** rc.4 and rc.5 rooms
   interoperate normally; **encrypted** rooms are opt-in and require a
   coordinated all-member upgrade to the rc.5 floor before writers are
   enabled, or the room partitions (reader-first rollout, spec D8).
4. The content-key rotation lifecycle's constraints are accepted as documented
   beta limitations: single-admin / admin-offline stall (T29), no automatic
   post-`member.left` rotation (T29), the `room_keys` secret living inside
   `rooms.db` (T28), and metadata-length leakage (T30).
5. The gossip overlay and its paired 40-member ceiling remain held back
   pending real-network overlay evidence, even though the loopback root cause
   is fixed.
6. The two D5a conflict-resolution durability defects (persistence and
   attribution) are fixed in this candidate.
7. The downstream `qualification/jeliya-*` pin channel is understood and its
   disposition decided — see `docs/operations/release-operations.md`. The pin
   predates all of #191 and should migrate to this candidate.

## Verdict

PENDING RELEASE-OWNER APPROVAL.
